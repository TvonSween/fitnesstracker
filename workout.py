class Workout:
    def __init__(self, name, workout_category, minutes):
        #super().__init__()
        #self.id = id
        self.name = name
        self.workout_category = workout_category
        self.minutes = minutes

Workout_categories  = ['Cardio', 'Flexibility', 'Strength: Upper body', 'Strength: Lower body', 'Balance']